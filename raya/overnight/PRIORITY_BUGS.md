# PRIORITY BUGS — grounded against real Raya transcripts (2026-07-30)

Built from the 8 static-analysis reports, each HIGH + notable-MED finding cross-checked against the bot's recent `raya_call.py` transcripts (tool-call `arguments` inspected, not just spoken content). **ACTIVE = a real recent call reproduces the failure; LATENT = static/prose risk only, the bot works in practice.**

## Grounding coverage (what the transcripts allowed)
- **Rich, groundable:** `kkb-hi-in` (b6222233, inbound — 15 calls, 3 get_profile / 1 create_profile / 7 apply_job), `kkb-kn-signals` (33037201 — 3 completed applies), `kkb-hi-signals` (115b38a5 — 2 completed applies), `dkb-kn` (d1a1614f — 1 completed create_job).
- **No repro possible (all recent calls `dur=0` undialed or short with ZERO tool calls):** `kkb-hi-out` (da612923, 07-24), `kkb-kn-out` (87ab9108), `maya-out` (47fdffe6), `dkb-hi` (57814ac8). Every OUTBOUND seeker/Maya bot's history is empty of completed tool flows → their findings can only be confirmed by a fresh live probe.
- **Phone double-prefix verdict (all bots):** NO `+91+91…` / `9191…` in ANY real call. up-getjob sends single `+91XXXXXXXXXX` (inbound caller_no is bare 10-digit → template prepends one +91); Signals sends single 12-digit `91XXXXXXXXXX` no `+`. The `${contact_phone}` on these deployments arrives bare → the D17/D39 landmine never fires in practice.

---

## Ranked priority list

| # | bot | sev | title | symptom (≤1 line) | D-pattern | ACTIVE / LATENT (+evidence) | proven-fix (which sibling solves it) | testable-via-harness | recommended-action |
|---|-----|-----|-------|-------------------|-----------|------------------------------|--------------------------------------|----------------------|--------------------|
| 1 | kkb-hi-in | HIGH | New-caller apply fails — no create_profile, fabricated profile_id | New inbound caller → `apply_job` sent `profile_id:"up-getjob"` / placeholder UUID with NO `create_profile` → HTTP 404 "Invalid or missing profile_id" | D31 / D16 / D28 | **ACTIVE** — `5449910e` (07-28, profile_id="up-getjob"→404 ×2), `34f1f587` (07-27, profile_id="a8a0e6f0-…-123456789abc"→404). Positive control: `1fde1677` did create_profile→apply and succeeded; returning callers `bedffaf5`/`8ddcaa5a` apply fine | YES — KKB base `kkb-kn-out` D31 (2026-07-27): split create→apply across a tool-result boundary; + D16 empty/no-profile ⇒ create_profile FIRST; forbid apply with fabricated/empty profile_id | inbound → needs-live-probe (historical repro already in hand) | PORT-proven-fix (D31 two-step + D16 create-first + forbid non-UUID profile_id), then repro→verify. NB residual tool-adherence component |
| 2 | kkb-hi-signals | HIGH | create/update_profile phone: 10-digit vs 12-digit contradiction | Stale bullet says pass bare 10-digit; template adds only `+` → malformed E.164 → wrong-user write | D39 / C3 / A4 | **LATENT** — get_profile + update_profile both send `917946350285` (correct 12-digit), apply succeeds; ev. `51b605a2`, `15e3f9d9`. Twin `kkb-kn-signals` update_profile phone=`917946350285` too (`b3ef7abe`). Model follows the 12-digit spec, not the stale bullet | YES — in-file: the correct 12-digit convention already stated one block below the stale bullet | outbound → yes | surgical-prose-fix (delete/align the stale 10-digit bullet; self-refuting, safe) — low urgency; batch with tonight's edits |
| 3 | kkb-hi-out | HIGH | `+91` hard-prepend get_profile/create_profile → double-prefix risk | If dialer `${contact_phone}` carries +91, template builds `+91+91…` → empty fetch / create 400 | D17 / C3 | **LATENT / no repro** — bot has zero completed tool calls (all `dur=0`/short); sibling `kkb-hi-in` (same up-getjob backend) sends single `+917862879115`/`+919108790249` — no double-prefix | YES — Maya outbound D17 (2026-07-20): exactly-one-+91 construction on both tools | outbound → yes | RUN-repro-call-then-decide (confirm dialer's `${contact_phone}` shape); PORT Maya fix if it carries +91 |
| 4 | kkb-hi-in | HIGH | Memory-resume opener may substitute for get_profile | Returning caller: speak name/role/journey from `${contact_memory}` before any fetch | D32 | **LATENT** — returning caller `8ddcaa5a` got the NEUTRAL greeting, `get_profile` fired turn 2, name spoken only AFTER fetch returned; no resume-opener in any call | PARTIAL — Signals clone D32 (07-29): neutral opener + "memory is NOT a fetch" guard | inbound → needs-live-probe | LATENT→open-items (two-turn fetch mandate holds); optionally add the memory-is-not-a-fetch guard defensively |
| 5 | kkb-kn-out | HIGH | Memory-resume opener may substitute for get_profile | Same as #4 on the outbound Kannada seeker | D32 | **LATENT / no repro** — 0 completed tool calls (all `dur=0`, one 16s hangup `e69015c5`) | PARTIAL — Signals clone D32 (07-29) | outbound → yes | LATENT→open-items / RUN-repro-call-then-decide |
| 6 | maya-hi | HIGH | get_profile `+91` hard-prepend → double-prefix on returning caller | Line 273 literal `+91${contact_phone}` vs the guarded create_profile block | D17 / C3 | **LATENT / no repro** — target agent (47fdffe6) has 0 tool calls (all `dur=0`/≤16s) | YES — this file's own create_profile block (line 730) + KKB family D17 | outbound → yes | RUN-repro-call-then-decide; surgical-prose-fix (align line 273 to the exactly-one-+91 form already at line 730) is cheap & safe |
| 7 | kkb-hi-in | MED | `hold_message` narrates the "silent" get_profile/create_profile | Revealing filler spoken over silent fetch/create | D34 | **ACTIVE** — get_profile `hold_message:"एक मिनट, आपकी जानकारी निकल रही है।"` and it is SPOKEN (`8ddcaa5a`); create_profile `hold_message:"…जानकारी बन रही है।"` spoken (`1fde1677`); greeting-prefixed lookup narration in `5449910e`/`34f1f587` | YES — Signals clones (07-29) set neutral hold ("एक मिनट।") for get_profile/create_profile; Signals transcripts confirm neutral holds live | inbound → needs-live-probe (historical repro in hand) | PORT-proven-fix (name `hold_message`, neutralise for silent tools) — good tonight candidate |
| 8 | dkb-kn | MED | `hold_message` non-empty + spoken on the silent create_job | create_job carries a spoken market-bridge as hold_message | D34 | **ACTIVE** — `cf3fc048` (07-04): `create_job … hold_message:"ಸರಿ, ನಾನು ಈಗ Hubli ನಲ್ಲಿ … eligible candidates ನೋಡ್ತೇನೆ."` then spoken verbatim next turn | YES — Signals D34 (07-29); DKB nuance: use empty `""` (DKB bans the "one moment" filler) | outbound → yes | PORT-proven-fix (empty hold_message for create_job/update_job_*); mirror Hi twin |
| 9 | dkb-kn (+ dkb-hi) | MED | Outbound bot invites callback in close (modality leak) | "ಖಂಡಿತ phone ಮಾಡಿ. Goodbye" — no inbound line exists | D5 | **ACTIVE** — `cf3fc048` close spoke it verbatim; shared with Hi ("…तो ज़रूर फोन करना। Goodbye") | none direct — apply D5 direction (center reaches out / post via platform) | outbound → yes | surgical-prose-fix (reframe close), sync Hi+Kn |
| 10 | dkb-kn (+ dkb-hi) | MED | `${phoneNumber}` undeclared → create_job phone = "Not Available" | Posted job carries no contact phone (var unbound) | C3 / C2 | **ACTIVE-non-breaking** — `cf3fc048`: `create_job … "phoneNumber":"Not Available"` yet backend returned `success:True, Payload accepted`. Write did NOT fail; data-quality gap only | partial — repo's single-+91 convention; declare `${phoneNumber}` | outbound → yes | surgical-prose-fix (declare + bind `${phoneNumber}`); confirm whether backend needs a real phone (else BACKEND-verify) |
| 11 | kkb-kn-signals | MED→ | apply_job on a `draft` item → 422 PROFILE_NOT_LIVE | Applied using the draft item_id as profile_id | D37 / D40 | **ACTIVE earlier, appears RESOLVED** — `eaa3f2d1` (07-29 **13:09**) failed 422 PROFILE_NOT_LIVE (applied on draft `2d1510d6`); later same-day `5804fd6b` (13:22) & `b3ef7abe` (18:36) apply on the LIVE profile `687fd5a1`/`300a4a0b` and succeed | YES — D37 live-item selection + D40 location-required (both claimed fixed 07-29) | outbound → yes | tool-schema-lever / BACKEND-verify: confirm `location` is in `create_profile.parameters.required` on the live agent config (prose gate alone is a D25 adherence risk) — do NOT add more prose |
| 12 | kkb-kn-signals | MED | create_profile phone 10-digit vs 12-digit (twin of #2) | Stale bullet at line 818 vs 12-digit at line 832 | D39 / C3 / A4 | **LATENT** — real calls send 12-digit `919108790249`/`917946350285`; ev. `5804fd6b`, `b3ef7abe` | YES — align stale bullet (twin of #2) | outbound → yes | surgical-prose-fix (batch with #2) |

*Not individually grounded (LATENT-by-construction, all lower than the above): kkb-hi-out D34/D5/D27, kkb-kn-out D34/D17/D36/D27, maya D34/D32/D36 — all on bots with no completed calls; dkb-hi D34/phone (no completed create flow); the various LOW hygiene items (example malformations, duplicate No-Match sections, D26 location-spelling gaps, glossary G1 backwards-binding). These are port-ready but blocked on a live probe or are cosmetic.*

---

## Recommended fix order

### Tonight — ACTIVE, repro→fix→verify loop
1. **kkb-hi-in D31/D16 — new-caller apply 404 (#1).** The only HIGH that hard-breaks a real flow: new inbound callers cannot apply (2 reproducing calls: `5449910e`, `34f1f587`). Port the `kkb-kn-out` D31 two-step (create→result-boundary→apply) + D16 "no/empty profile ⇒ create_profile FIRST" + an explicit ban on `apply_job` with a non-UUID / fabricated `profile_id`. Verify with a fresh new-inbound-caller probe. (Keep in mind a runtime tool-adherence residue — if the model still skips create after hardening, escalate as a platform/tool-schema backstop, don't pile on prose.)
2. **kkb-hi-in D34 — hold_message reveal (#7).** ACTIVE and already solved on the Signals clones (neutral holds proven live). Straight port; verify the get_profile/create_profile hold_message goes neutral on the next inbound call.
3. **dkb-kn D34 + D5 + phoneNumber (#8/#9/#10).** All three confirmed in one real call (`cf3fc048`); all prose/config fixes; sync Hi+Kn. (D5 and D34 are user-facing; phoneNumber is data-quality since the write still succeeds.)

### Verify, don't edit blindly
4. **kkb-kn-signals PROFILE_NOT_LIVE (#11).** Confirm the D40 `location`-required tool-schema actually stuck on the live agent config (one 07-29 call still failed pre-fix). This is a schema/backend check, not a prose edit.

### Cheap prose hygiene — batch anytime (LATENT, safe)
5. **Phone 10-vs-12 stale bullets (#2, #12)** and **maya line 273 (#6)** — self-refuting contradictions; align to the correct form already present in each file. Removes the landmine even though it isn't firing today.

### Open-items list (LATENT / no repro — need a live probe before touching)
6. **kkb-hi-out D17 (#3), kkb-kn-out D32 (#5), maya D17/D32/D36, kkb-hi-in D32 (#4).** Every outbound seeker/Maya bot has an empty tool-call history, so nothing here is confirmed. Flag for the user: run a targeted live probe (returning caller whose dialer `${contact_phone}` carries `+91`; returning caller with prior `${contact_memory}`) to decide ACTIVE vs defer. Fixes are port-ready (Maya D17, Signals D32) if a probe confirms.
